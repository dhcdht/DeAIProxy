


# DeAI Proxy Network (DeAPN): 去中心化 AI API 算力与信用流转协议
**核心架构与经济学白皮书 (v2.0)**

---

## 1. 摘要 (Abstract)
DeAI Proxy Network (DeAPN) 是一个基于密码学验证与博弈论的去中心化 AI API 共享网络。本协议旨在解决当前 AI 大模型市场中广泛存在的“资源闲置与访问壁垒”问题。通过整合**本地反向代理 (Local Reverse Proxy)**、**零知识传输层安全 (zkTLS)**、**状态通道实时结算 (State Channel Billing)** 以及**基于质押的仲裁机制 (Staking & Slashing Dispute)**，DeAPN 构建了一个无需信任、抗审查且规避官方风控策略的点对点 AI 资源流转市场。

---

## 2. 行业痛点与问题定义 (Problem Statement)
当前 AI API（如 OpenAI, Anthropic）的二次分发与共享面临四大系统性障碍：
1. **ToS 风控与封号风险**：中心化代理代管 API Key 极易因跨国 IP 聚集和高并发触发官方的反滥用风控（导致封号及信用卡拉黑）。
2. **零信任环境下的验证难题（Oracle Problem）**：买方（特别是 AI Agent）无法验证卖方返回的数据是否真正来自官方闭源大模型，卖方有动机使用低成本开源小模型（如 Llama-8B）进行伪装欺诈。
3. **流式响应与链上结算的矛盾**：AI 推理按 Token 流式计费，若每次请求均触发链上智能合约结算，极高的 Gas 成本将彻底摧毁商业可行性。
4. **劣币驱逐良币与买方白嫖**：缺乏基于经济惩罚的双向制衡机制，导致卖家提供低质服务，或买家恶意发起退款差评。

---

## 3. 核心系统架构 (System Architecture)

系统由三层架构组成，实现数据流与资金流的物理隔离。

### 3.1 卖方供应层：本地代理节点 (Local Proxy Node)
* **无代管运行 (Non-custodial)**：卖方在个人 PC 或云服务器上运行轻量级节点客户端。API Key 仅加密储存于本地环境变量，**绝对不上链、不传平台服务器**。
* **原生 IP 维稳 (IP Masking)**：所有向官方模型发起的 TLS 请求，均从卖方本地网络（如家庭宽带）发出，自然规避大厂的集中式 IP 风控。
* **按需定价**：卖方可在客户端自定义折扣率（如基准官方价的 60%）及最大并发限制。

### 3.2 买方调用层：统一网关与 SDK (Client Layer)
* **无缝迁移**：提供兼容 OpenAI 格式的 SDK 端点（Base URL）。开发者或 AI Agent 代码只需替换 Base URL，并使用链上钱包签名作为鉴权凭证，无需修改核心业务代码。
* **并发打散**：针对买方的高并发需求，系统自动将请求拆解并路由至网络中多个相互独立的卖方节点，突破单一 API Key 的速率限制（Rate Limits）。

### 3.3 路由与调度层 (Smart Routing Layer)
* 初期采用高性能集群负责请求派发，后期向 libp2p 的分布式哈希表（DHT）网络演进。
* 路由算法基于动态权重：优先派单给**“延迟低 + 历史在线率高 + 质押金充足 + 价格低”**的卖方节点。

---

## 4. 信任机制：质量评价与博弈仲裁 (Dispute & Game Theory)

为解决“假模型欺诈”与“买家恶意差评白嫖”，协议摒弃了每单强制 zkTLS 验证（过重、延迟极高），采用**“乐观验证 (Optimistic Rollup) + 随机/按需 zkTLS 仲裁”**的三体博弈模型。

### 4.1 自动化客观评分 (Passive Scoring)
买方 SDK 在后台静默记录并上报三大指标：**首字延迟 (TTFT)**、**传输吞吐量 (TPS)**、**连接中断率 (Drop Rate)**。路由层据此动态更新卖方节点的健康度分值（Health Score），作为派单权重的基准。

### 4.2 主动举报与冻结 (Active Dispute)
如果买方（开发者或 AI Agent）识别到响应内容逻辑极度劣质或格式严重损坏（疑似遭遇小模型替换作弊），买方有权在响应结束后的规定时间窗内，通过智能合约调用 `report_fraud()` 发起仲裁。此时，该笔交易的资金将被合约冻结。

### 4.3 基于 zkTLS 的终极仲裁庭 (The Arbitrator)
一旦买方发起举报，协议进入基于密码学的零知识证明验证流程：
1. **举证要求**：被举报的卖方节点必须在 3 分钟内，利用本地保留的连接会话记录，生成一份 **zkTLS 证明 (Zero-Knowledge TLS Proof)** 并提交至链上。
2. **验证逻辑**：zkTLS 证明能够通过密码学证实：“该本地节点确与 `api.openai.com` 建立了 TLS 连接，且被举报的响应文本 100% 为官方服务器返回，未受本地篡改”。
3. **判定与罚没 (Slashing)**：
   * **卖方作恶判定 (情况 A)**：卖方无法提交有效的 zkTLS 证明（确系伪造或本地替换模型）。判定买方胜诉，退还请求费，并从卖方质押的保证金中**扣除 10-50 倍的罚金 (Slash) 赔付给买方**。
   * **买方恶意白嫖判定 (情况 B)**：卖方成功提交 zkTLS 证明，证实返回的确实是官方原始数据。判定卖方胜诉，强制执行付款。同时，**买方需向卖方支付一笔高昂的“恶意仲裁违约金”**，并大幅降低该买方钱包地址的信用评级。

**结论**：通过严厉的不对称经济惩罚，构建了“黑暗森林法则”，在 99% 的无争议调用中避免了生成 zkTLS 的性能开销，实现了极低延迟，同时确保了系统的绝对诚实。

---

## 5. 经济模型与链下结算 (Tokenomics & Settlement)

为解决流式按 Token 计费引发的以太坊/L2 Gas 爆炸问题，协议采用双代币模型与状态通道结算机制。

### 5.1 链下状态通道记账 (Micro-payment Channels)
1. **预存与锁定**：买方向智能合约质押一定数量的稳定币（如 USDC）作为可用余额。
2. **链下高频记账**：买卖双方的每一次请求、Token 消耗均在链下路由节点以状态签名（State Signatures）的形式累计。
3. **批量结算 (Rollup/Settlement)**：按周期（如每 24 小时）或买方主动断开连接时，将最终账单打包提交至链上智能合约，进行一次性清算与资金划拨。

### 5.2 双代币经济体系
* **流通代币 (USDC/USDT)**：系统内 AI 服务的计价和结算货币，保障商业客户预算的可预测性。
* **协议治理与权益代币 ($PROXY)**：
  * **节点质押门槛 (Staking)**：卖方必须质押一定数量的 $PROXY 才能接入网络接单。质押量不仅是其信誉的担保，也决定了其所能承接并发量的上限。
  * **冷启动激励 (Liquidity Mining)**：在协议早期，为鼓励卖方提供闲置 API Key 并保持节点在线，系统将根据节点的在线时长和备用带宽，发放 $PROXY 作为流动性激励（即“挂机挖矿”）。
  * **治理投票**：持有者可投票决定协议抽成比例、恶意行为的 Slash 乘数等关键系统参数。

---

## 6. 技术实施路线图 (Roadmap)

### Phase 1: 最小可行性网络 (MVP) - "Centralized Router, Decentralized Nodes"
* **目标**：验证产品市场契合度 (PMF) 与本地防风控机制。
* **交付物**：基于开源 `LiteLLM/Apex` 魔改的卖方桌面/CLI 客户端；支持稳定币存款的以太坊 L2 智能合约；由官方运行的中心化高并发路由网关与链下记账服务器。

### Phase 2: 信任最小化与仲裁上线 - "The Game Theory Upgrade"
* **目标**：实装防作恶机制。
* **交付物**：将 TLSNotary 或相应 zkTLS 库集成至卖方客户端；上线智能合约仲裁模块（Dispute Engine）；引入客观评分系统与罚没机制 (Slashing)；发布 $PROXY 协议代币用于质押。

### Phase 3: 完全去中心化 - "P2P AI Proxy Network"
* **目标**：彻底消除单点故障，实现抗审查。
* **交付物**：废弃中心化路由服务器；引入基于 libp2p 的分布式哈希表网络；全节点既是供应方也是路由中继方；实现针对 AI Agent 原生钱包的微支付无缝集成。